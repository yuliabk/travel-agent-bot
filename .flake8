[flake8]
max-line-length = 120
# E203 and W503 conflict with black's formatting; E501 line length is owned by black
# (black intentionally leaves long strings/URLs untouched).
extend-ignore = E203, W503, E501
exclude =
    .git,
    __pycache__,
    .venv,
    venv,
    logs,
    build,
    dist
